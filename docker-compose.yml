version: '2.1'

services:
  users_db:
    image: nouchka/sqlite3:latest
    stdin_open: true
    tty: true
    volumes:
      - ./db/:/root/db/
  users_ms:
    image: fgomezl/register_and_auth_ms
    depends_on:
      - users_db
    ports:
      - "3001:3001"

